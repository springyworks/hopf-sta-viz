{
  "name": "hopf-sta-viz-web-demo",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript source for the GitHub Pages artist's-impression demo. Compiles to ../docs/main.js.",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
