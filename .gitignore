# Rust / Cargo build output
/target/

# Trunk wasm staging output (the publishable copy lives in docs/)
/dist/

# Runtime artifacts written by the app into the working directory
# (timestamped preset dumps, and the in-app F1 remark log)
/preset-*.json
/ui-remarks.json

# Node tooling for the web/ demo source (typescript is the only devDependency)
node_modules/

# Editor / OS cruft
*.swp
*~
.DS_Store
.idea/
.vscode/
